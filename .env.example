# Optional: Web mode supports normal SoundCloud login and playback without API credentials.
# API mode can use a registered SoundCloud app with this redirect URI:
# http://127.0.0.1:42871/soundcloud/callback
SOUNDCLOUD_CLIENT_ID=your_client_id
SOUNDCLOUD_CLIENT_SECRET=your_client_secret
AURALIS_AUTH_PORT=42871
